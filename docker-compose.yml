services:
  postgres:
    image: postgres:16
    container_name: postgres-lab
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: labdb
    ports:
      - "5432:5432"